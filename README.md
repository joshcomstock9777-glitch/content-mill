# Content Mill

Local production room. Open `content-mill.html` in a browser.

No Replit. No npm. No push required to run.

Flow: idea → rights → script → package → approve → export JSON.

Jobs save in the browser. Approved jobs download as a package HQ can ingest later.
